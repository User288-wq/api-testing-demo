{
  "name": "api-testing-demo",
  "version": "1.0.0",
  "description": "Tests API avec Postman/Newman",
  "scripts": {
    "test:dev": "newman run tests/api/collections/api-test-suite.json -e tests/api/environments/dev.json -r cli,htmlextra --reporter-htmlextra-export reports/dev-report.html",
    "test:staging": "newman run tests/api/collections/api-test-suite.json -e tests/api/environments/staging.json -r cli,htmlextra --reporter-htmlextra-export reports/staging-report.html",
    "test:all": "npm run test:dev && npm run test:staging"
  },
  "devDependencies": {
    "newman": "^6.1.0",
    "newman-reporter-htmlextra": "^1.23.0"
  }
}
